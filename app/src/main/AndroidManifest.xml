<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.xingnext.bigdata">

    <!-- 访问网络 -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 写外部存储 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 获取运营商信息，用于支持提供运营商信息相关的接口 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 访问wifi网络信息,wifi信息可用于进行网络定位 -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- 获取wifi的获取权限,wifi信息可用来进行网络定位 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <!-- 读取手机当前的状态 -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <!-- 拨打电话 -->
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppRedTheme">
        <activity
            android:name=".StartActivity"
            android:theme="@style/AppNoBarTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".GameDetailActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".GameRecomDetailActivity"
            android:theme="@style/AppTheme" />
        <activity android:name=".BuyShaobingActivity" />
        <activity android:name=".MyreadRecordActivity" />
        <activity android:name=".MyAccountActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistActivity" />
        <activity android:name=".ResetPasswordActivity" />
        <activity android:name=".OrderDetailActivity" />
        <activity android:name=".CouponWebActivity" />
        <activity android:name=".PayWebActivity" />
        <activity android:name=".AboutWebActivity" />
        <activity android:name=".GameSampleActivity" />
        <activity android:name=".GifWebActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppNoBarTheme" />
        <activity android:name=".RechareWebActivity"></activity>
    </application>

</manifest>